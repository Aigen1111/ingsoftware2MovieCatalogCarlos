<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container">
    <%- include('partials/navbar') %>
    <h1 class="display-4 mb-4">Admin Dashboard</h1>
    <section class="mb-4">
      <h2 class="h4">Estadísticas Generales</h2>
      <p>Total de Películas: <%= totalMovies %></p>
      <h3 class="h5 mt-3">Películas Mejor Puntuadas</h3>
      <ul class="list-group">
        <% topRatedMovies.forEach(movie => { %>
          <li class="list-group-item"><%= movie.title %> - <%= movie.ratings.length %> ratings</li>
        <% }) %>
      </ul>
    </section>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>